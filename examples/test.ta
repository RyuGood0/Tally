def int add(a, b) { # adds two numbers together
    return a+b
}

def float divide(float a, b) {
    return a/b
}

a = 1.5
a++
int b = 3
str c = "hello world"

print(add(a, b))
print(divide(a, b))

if a > b {
    print("a is larger")
}